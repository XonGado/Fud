export interface Item{
	item_id: string,
	item_name: string,
	item_description: string,
	item_price: string,
	item_type: string,
	item_count: number,
	item_ordered: number,
	item_availability: boolean,
	item_visibility: boolean,
	cleared: boolean
}